import gzip
import pickle
from pathlib import Path

import ecole
import numpy as np
import torch
import torch.nn.functional as F
import torch_geometric

# Que es el dataset

# Para que es el dataset

# Que cosas contiene el dataset
    # Par(estado, accion)
    # estado:
        #
